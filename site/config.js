// Landing-page settings. Edit here; nothing else needs to change.
window.LUMIN_CONFIG = {
  // Meta Events Manager → Data sources → your Pixel → "Pixel ID" (digits only).
  // Empty = no Meta script is loaded at all (no tracking, no cookie).
  metaPixelId: "",
  webAppUrl: "https://app.luminapp.org/",
  playUrl: "https://play.google.com/store/apps/details?id=org.luminapp.lumin",
};
